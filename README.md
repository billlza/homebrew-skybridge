# homebrew-skybridge

Homebrew tap for SkyBridge CLI.

## Install

```bash
brew tap billlza/skybridge
brew install skybridge
```

The `skybridge.rb` formula is updated from the main SkyBridge Compass release workflow.
